<div class="container">
  <div class="row">
    <div class="col-xs-10">
      <div class="col-xs-1 grav-top-pad">
        <%= link_to gravatar_for(@article.user), user_path(@article.user) %>
        <small><%= pluralize(@article.user.articles.count, "article") if @article.user.articles %></small>
      </div>
      <div class="col-xs-11">
        <h2><%= @article.title %></h2>
      </div>
    </div>
  </div>
<div class="container">
  <div class="col-xs-10">
    <% if @article.user %>

    <h4><%= link_to @article.title, "#{@article.link}", target: "_blank" %></h4>

    <% end %>
  </div>
</ul
